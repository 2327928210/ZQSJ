<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.haier.sheji">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="design_width"
            android:value="720" />
        <meta-data
            android:name="design_height"
            android:value="1280" />

        <activity android:name=".WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="homepager.HomePagerActivity" />
        <activity android:name="myself.MyselfActivity" />
        <activity android:name=".BaseActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".find.Find_Second_Activity1" />
        <activity android:name=".find.Find_Second_Activity2" />
        <activity android:name=".find.FindHeadViewActivity1" />
        <activity android:name=".find.FindHeadViewActivity2" />
        <activity android:name=".find.FindHeadViewActivity3" />
        <activity android:name=".find.FindThridActivity1" />
        <activity android:name=".find.FindThridActivity2"></activity>
    </application>

</manifest>